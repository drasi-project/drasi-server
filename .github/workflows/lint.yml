name: Lint

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - improve-drasi-server-workflows

jobs:
  lint:
    uses: drasi-project/.github/.github/workflows/rust-lint.yaml@main
