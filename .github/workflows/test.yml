name: Test

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - improve-drasi-server-workflows

jobs:
  test:
    uses: drasi-project/.github/.github/workflows/rust-unit-test.yaml@main
