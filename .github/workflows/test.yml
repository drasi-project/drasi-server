name: Test

on:
  pull_request:

jobs:
  test:
    uses: drasi-project/.github/.github/workflows/rust-unit-test.yaml@main
    with:
      install-jq: true
      install-protobuf: true
      test-command: |
        make test-tooling
        cargo test --locked
