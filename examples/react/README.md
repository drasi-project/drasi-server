# React examples moved

The canonical workspace now lives beside the components at
[`dev-tools/react/examples`](../../dev-tools/react/examples/README.md).
Run its documented commands from that location. This directory retains only
this link for earlier documentation; it is not a second application or a
runtime alias.
